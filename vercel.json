{
  "version": 2,
  "buildCommand": "npm run prisma:generate && npm run seed --workspace server && npm run build --workspace web",
  "installCommand": "npm install",
  "framework": "nextjs",
  "outputDirectory": "apps/web/.next",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/server"
    }
  ]
}
