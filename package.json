{
  "name": "osta",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web",
    "dev:web": "npm run dev --workspace web",
    "dev:server": "npm run dev --workspace server",
    "build": "npm run prisma:generate && npm run prisma:push && npm run build --workspace web",
    "lint": "npm run lint --workspace web && npm run lint --workspace server",
    "typecheck": "npm run typecheck --workspace web && npm run typecheck --workspace server",
    "prisma:generate": "npm run prisma:generate --workspace server",
    "prisma:push": "npm run prisma:push --workspace server",
    "seed": "npm run seed --workspace server"
  }
}
